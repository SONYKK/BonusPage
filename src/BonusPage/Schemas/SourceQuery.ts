export type SourceQuery = SourceQueryEnum.ZERO | SourceQueryEnum.ONE |
                        SourceQueryEnum.TWO | SourceQueryEnum.THREE

enum SourceQueryEnum {
    ZERO = 0,
    ONE = 1,
    TWO = 2,
    THREE = 3
}
